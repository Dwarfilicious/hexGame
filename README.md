Tile selecting is now functional and there is a way to
export and import maps from a JSON. There is only a
basic implementation of map generation (also wrongly named,
as it does not produce a pangaea map).
